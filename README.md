# Jenkins CI/CD Demo (Windows)

This project demonstrates a Jenkins CI/CD pipeline running locally on Windows.

## Steps
1. Install Java 17
2. Run Jenkins (jenkins.war)
3. Install Docker Desktop
4. Create Jenkins Pipeline job
5. Build and run

Access app: http://localhost:5000
